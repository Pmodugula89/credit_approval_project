# credit_approval_project
Logistic regression model to predict credit approval. Includes EDA, preprocessing (imputation, encoding, scaling), model training, odds ratio interpretation, threshold tuning, and evaluation. Built in Python with scikit-learn and VS Code.
